#include <iostream>
#include <cmath>
#include <algorithm>
#include <iomanip> 
#include <string>
using namespace std;

int main(){
    int n;
    cin >> n;
    int i = 1;
    if (n == 0) {
        cout << 1 << endl;
        return 0;
    }
    long long fact = 1;
    for (int i = 1; i <= n; ++i){
            fact *= i;
        }
    cout << fixed << setprecision(2) << fact << endl;
    return 0;
}